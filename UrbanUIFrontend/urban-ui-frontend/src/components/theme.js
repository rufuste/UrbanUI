import { createTheme } from '@mui/material/styles';
import {
    blueberryTwilightPalette,
    mangoFusionPalette,
    cheerfulFiestaPalette,} from '@mui/x-charts/colorPalettes';

    
const theme = createTheme({
    palette: cheerfulFiestaPalette
});

export default theme;